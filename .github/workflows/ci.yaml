name: ci
on: [push, pull_request]
jobs:
  lint:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v4
    - name: Lint
      run: |
        xcrun swift-format lint . \
          --parallel \
          --recursive \
          --strict
